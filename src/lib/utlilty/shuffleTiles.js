export function shuffleTiles(words, i) {
	// pushWord: string to characters in an array
	// shuffleLetters: shuffle letters, return as split
	pickedWord.text = shuffleLetters(pushWord(pickedWord.text), true);
}
